import pw from '/root/.claude/skills/playwright-skill/node_modules/playwright-core/index.js';
import { execSync } from 'node:child_process';
import { existsSync } from 'node:fs';
const { chromium } = pw;
const exe = ['/root/.cache/ms-playwright/chromium_headless_shell-1228/chrome-headless-shell-linux64/chrome-headless-shell','/snap/bin/chromium'].find(existsSync);
const AUTH = 'opencode:f14dd6828c5bcd6bd35d14be281c3e79802b002ddabd2bd7';
const SID = 'ses_0114cc2caffeD3ZpcCcuxcGnzR';
const browser = await chromium.launch({ executablePath: exe, headless: true, args: ['--no-sandbox','--disable-gpu'] });
const page = await browser.newPage({ viewport: { width: 430, height: 900 } });
const logs = [];
page.on('console', m => logs.push(`[${m.type()}] ${m.text()}`));
page.on('pageerror', e => logs.push(`[error] ${e.message}`));
await page.goto('http://127.0.0.1:9928/pulse', { waitUntil:'load', timeout:120000 });
await page.waitForTimeout(7000);
await page.locator('[data-testid^="project-"]').first().dispatchEvent('click',{bubbles:true});
await page.waitForTimeout(6000);
const msg = 'live-debug-' + Date.now().toString().slice(-6);
console.log('>>> 发送:', msg);
execSync(`curl -s -u "${AUTH}" -X POST "http://127.0.0.1:4096/session/${SID}/prompt_async" -H "Content-Type: application/json" -d '{"parts":[{"type":"text","text":"${msg}"}]}'`, { timeout: 15000 });
await page.waitForTimeout(8000);
console.log('=== ChatPanel SSE 日志 ===');
logs.filter(l => l.includes('[ChatPanel]')).slice(0,20).forEach(l => console.log(l));
console.log('=== 其他关键日志 ===');
logs.filter(l => l.includes('SSE') || l.includes('error') || l.includes('fetch')).slice(0,10).forEach(l => console.log(l));
console.log('=== 页面包含测试消息? ===');
console.log(await page.evaluate((m) => document.body.textContent.includes(m), msg));
await browser.close();
