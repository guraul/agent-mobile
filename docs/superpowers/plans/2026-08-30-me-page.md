# Me 页实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把占位的 Me tab 做成 Jarvis 式「配置/个人中心」翼——账号登出、BFF 地址运行时切换(重启生效)、按 agent 配默认 model。

**Architecture:** 方案 C(重启生效)——BFF 地址写 AsyncStorage,启动读一次设 `runtimeBaseUrl`,运行中不热切;model 偏好走手机端覆盖模式(存 AsyncStorage,ChatPanel 读偏好设默认 pill,发消息带 `model` 覆盖);Me 页 3 个 Card(连接与账号 / BFF 地址 / model 偏好)。

**Tech Stack:** Expo SDK 57 / React Native 0.86 / TypeScript / vitest / AsyncStorage

## Global Constraints

- **Expo SDK 57**:依赖用 `npx expo install` 装,版本匹配 SDK 57;改 agent-mobile-app 代码前读 `https://docs.expo.dev/versions/v57.0.0/`
- **验证**:改完 TS 必须 `cd agent-mobile-app && pnpm exec tsc --noEmit`;纯逻辑改动必须 `pnpm test`(vitest,`src/**/*.test.ts`)
- **颜色/字号/间距**走 `src/theme/`,不硬编码;**勿改** `StatusType` 取值集合(`running|idle|success|error|warning`)
- **临时脚本/截图**放仓库根 `test/`,勿散落 /tmp
- **AsyncStorage 已是依赖**(`auth.ts` 在用),无需新装
- **AsyncStorage mock 模板**(凡涉及 AsyncStorage 的 test 文件顶部用):
```ts
import { vi, describe, it, expect, beforeEach } from "vitest";
const { store } = vi.hoisted(() => ({ store: new Map<string, string>() }));
vi.mock("@react-native-async-storage/async-storage", () => ({
  default: {
    getItem: (k: string) => Promise.resolve(store.get(k) ?? null),
    setItem: (k: string, v: string) => { store.set(k, v); return Promise.resolve(); },
    removeItem: (k: string) => { store.delete(k); return Promise.resolve(); },
  },
}));
beforeEach(() => store.clear());
```

---

### Task 1: bff-config 服务 + getBaseUrl 定义

**Files:**
- Modify: `agent-mobile-app/src/config/opencode.ts`
- Create: `agent-mobile-app/src/services/bff-config.ts`
- Test: `agent-mobile-app/src/services/bff-config.test.ts`

**Interfaces:**
- Produces: `getBaseUrl(): string`(config 导出,同步,优先 `runtimeBaseUrl` 回退 env `baseUrl`);`getRuntimeBaseUrl()/setRuntimeBaseUrl(url)/clearRuntimeBaseUrl()`(bff-config,异步,AsyncStorage key `pulse_bff_url`)

- [x] **Step 1: 写失败测试**

`src/services/bff-config.test.ts`:
```ts
import { vi, describe, it, expect, beforeEach } from "vitest";
const { store } = vi.hoisted(() => ({ store: new Map<string, string>() }));
vi.mock("@react-native-async-storage/async-storage", () => ({
  default: {
    getItem: (k: string) => Promise.resolve(store.get(k) ?? null),
    setItem: (k: string, v: string) => { store.set(k, v); return Promise.resolve(); },
    removeItem: (k: string) => { store.delete(k); return Promise.resolve(); },
  },
}));
beforeEach(() => store.clear());

import { getRuntimeBaseUrl, setRuntimeBaseUrl, clearRuntimeBaseUrl } from "./bff-config";
import { opencodeConfig, getBaseUrl } from "../config/opencode";

describe("bff-config", () => {
  it("setRuntimeBaseUrl 持久化且 getRuntimeBaseUrl 读回", async () => {
    await setRuntimeBaseUrl("http://1.2.3.4:5678");
    expect(await getRuntimeBaseUrl()).toBe("http://1.2.3.4:5678");
  });

  it("clearRuntimeBaseUrl 清除", async () => {
    await setRuntimeBaseUrl("http://x:1");
    await clearRuntimeBaseUrl();
    expect(await getRuntimeBaseUrl()).toBeNull();
  });
});

describe("getBaseUrl", () => {
  beforeEach(() => { opencodeConfig.runtimeBaseUrl = null; });

  it("无覆盖时回退 env baseUrl", () => {
    expect(getBaseUrl()).toBe(opencodeConfig.baseUrl);
  });

  it("有覆盖时优先 runtimeBaseUrl", () => {
    opencodeConfig.runtimeBaseUrl = "http://override:9";
    expect(getBaseUrl()).toBe("http://override:9");
  });
});
```

- [x] **Step 2: 运行测试确认失败**

Run: `cd agent-mobile-app && pnpm test -- src/services/bff-config.test.ts`
Expected: FAIL `Cannot find module './bff-config'` / `getBaseUrl is not a function`

- [x] **Step 3: 实现**

`src/config/opencode.ts`(覆盖全文):
```ts
export const opencodeConfig = {
  baseUrl:
    process.env.EXPO_PUBLIC_OPENCODE_URL ??
    "http://106.13.181.13:19234",
  runtimeBaseUrl: null as string | null,
  token: "",
};

/** 运行时覆盖优先(启动时从 AsyncStorage 读),否则回退 env 默认 */
export function getBaseUrl(): string {
  return opencodeConfig.runtimeBaseUrl ?? opencodeConfig.baseUrl;
}
```

`src/services/bff-config.ts`:
```ts
import AsyncStorage from "@react-native-async-storage/async-storage";

const KEY = "pulse_bff_url";

export async function getRuntimeBaseUrl(): Promise<string | null> {
  try { return await AsyncStorage.getItem(KEY); } catch { return null; }
}

export async function setRuntimeBaseUrl(url: string): Promise<void> {
  try { await AsyncStorage.setItem(KEY, url); } catch {}
}

export async function clearRuntimeBaseUrl(): Promise<void> {
  try { await AsyncStorage.removeItem(KEY); } catch {}
}
```

- [x] **Step 4: 运行测试确认通过**

Run: `cd agent-mobile-app && pnpm test -- src/services/bff-config.test.ts`
Expected: PASS (4 tests)

- [x] **Step 5: tsc + 提交**

Run: `cd agent-mobile-app && pnpm exec tsc --noEmit`
```bash
cd agent-mobile-app
git add src/config/opencode.ts src/services/bff-config.ts src/services/bff-config.test.ts
git commit -m "feat: bff-config 服务 + getBaseUrl 运行时覆盖(方案C)"
```

---

### Task 2: auth.ts 改全(login 用 getBaseUrl+存 username;新增 setUsername/getUsername/clearUsername/logout)

**Files:**
- Modify: `agent-mobile-app/src/services/auth.ts`
- Test: `agent-mobile-app/src/services/auth.test.ts`

**Interfaces:**
- Consumes: `getBaseUrl` from Task 1
- Produces: `setUsername(u)/getUsername(): Promise<string|null>/clearUsername()`;`logout()`(POST `/api/auth/logout` + 清 token/username);`login` 成功时调 `setUsername`

- [x] **Step 1: 写失败测试**

`src/services/auth.test.ts`:
```ts
import { vi, describe, it, expect, beforeEach } from "vitest";
const { store } = vi.hoisted(() => ({ store: new Map<string, string>() }));
vi.mock("@react-native-async-storage/async-storage", () => ({
  default: {
    getItem: (k: string) => Promise.resolve(store.get(k) ?? null),
    setItem: (k: string, v: string) => { store.set(k, v); return Promise.resolve(); },
    removeItem: (k: string) => { store.delete(k); return Promise.resolve(); },
  },
}));
beforeEach(() => store.clear());

import { setUsername, getUsername, clearUsername } from "./auth";

describe("username 持久化", () => {
  it("setUsername 存 getUsername 读回", async () => {
    await setUsername("admin");
    expect(await getUsername()).toBe("admin");
  });

  it("clearUsername 清除", async () => {
    await setUsername("admin");
    await clearUsername();
    expect(await getUsername()).toBeNull();
  });
});
```

- [x] **Step 2: 运行确认失败**

Run: `cd agent-mobile-app && pnpm test -- src/services/auth.test.ts`
Expected: FAIL `setUsername is not a function`

- [x] **Step 3: 实现**

`src/services/auth.ts` 在现有 `TOKEN_KEY` 后加 `USERNAME_KEY` + 新函数;改 `login` 用 `getBaseUrl` + 存 username;`handleUnauthorized` 清 username;新增 `logout`:

在 `const TOKEN_KEY = ...` 下加:
```ts
const USERNAME_KEY = "pulse_username";

export async function getUsername(): Promise<string | null> {
  try { return await AsyncStorage.getItem(USERNAME_KEY); } catch { return null; }
}

export async function setUsername(username: string): Promise<void> {
  try { await AsyncStorage.setItem(USERNAME_KEY, username); } catch {}
}

export async function clearUsername(): Promise<void> {
  try { await AsyncStorage.removeItem(USERNAME_KEY); } catch {}
}
```

改 import(顶部):
```ts
import { opencodeConfig, getBaseUrl } from "../config/opencode";
```

改 `login`(用 `getBaseUrl()` + 成功存 username):
```ts
export async function login(username: string, password: string): Promise<string> {
  const res = await fetch(`${getBaseUrl()}/api/auth/login`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ username, password }),
  });
  if (!res.ok) {
    const body = await res.json().catch(() => ({}));
    throw new Error(body?.error ?? `login failed: ${res.status}`);
  }
  const { token } = await res.json();
  if (!token) throw new Error("login returned no token");
  await setToken(token);
  await setUsername(username);
  return token;
}
```

改 `handleUnauthorized`(加清 username):
```ts
export async function handleUnauthorized(): Promise<void> {
  await setToken(null);
  await clearUsername();
  unauthorizedCb?.();
}
```

新增 `logout`(文件末尾):
```ts
// 登出:通知 BFF + 清本地 token/username(失败仍清本地,保证本地登出)
export async function logout(): Promise<void> {
  try {
    await fetch(`${getBaseUrl()}/api/auth/logout`, {
      method: "POST",
      headers: tokenHeader(),
    });
  } catch {
    // 网络失败——仍清本地,Me 页提示"服务端登出失败,已本地登出"
  }
  await setToken(null);
  await clearUsername();
}
```

- [x] **Step 4: 运行确认通过**

Run: `cd agent-mobile-app && pnpm test -- src/services/auth.test.ts`
Expected: PASS (2 tests)

- [x] **Step 5: tsc + 提交**

Run: `cd agent-mobile-app && pnpm exec tsc --noEmit`
```bash
cd agent-mobile-app
git add src/services/auth.ts src/services/auth.test.ts
git commit -m "feat: auth 持久化 username + logout 端点,login 走 getBaseUrl"
```

---

### Task 3: 其余 4 处读处改 getBaseUrl

**Files:**
- Modify: `agent-mobile-app/src/services/opencode-client.ts:83`
- Modify: `agent-mobile-app/src/services/opencode-events.ts:149`
- Modify: `agent-mobile-app/src/services/fund-events.ts:47,82`

**Interfaces:**
- Consumes: `getBaseUrl` from Task 1
- 无新测试(纯替换,靠 tsc + 已有测试)

- [x] **Step 1: opencode-client.ts**

import 加 `getBaseUrl`:
```ts
import { opencodeConfig, getBaseUrl } from "../config/opencode";
```
line 83 改:
```ts
const res = await fetch(`${getBaseUrl()}/api/opencode/rest${path}`, {
```

- [x] **Step 2: opencode-events.ts**

import 加 `getBaseUrl`(若已 import opencodeConfig 则改为带 getBaseUrl;否则新增 import):
```ts
import { getBaseUrl } from "../config/opencode";
```
line 149 改:
```ts
const res = await fetch(`${getBaseUrl()}/api/opencode/stream${qs}`, {
```

- [x] **Step 3: fund-events.ts**

import 加 `getBaseUrl`:
```ts
import { opencodeConfig, getBaseUrl } from "../config/opencode";
```
line 47(ackTradeAlert)改:
```ts
await fetch(`${getBaseUrl()}/api/events/ack`, {
```
line 82(stream 订阅)改:
```ts
const res = await fetch(`${getBaseUrl()}/api/events/stream`, {
```

- [x] **Step 4: tsc + 已有测试**

Run: `cd agent-mobile-app && pnpm exec tsc --noEmit && pnpm test`
Expected: tsc 0 errors;全部测试通过(无回归)

- [x] **Step 5: 提交**

```bash
cd agent-mobile-app
git add src/services/opencode-client.ts src/services/opencode-events.ts src/services/fund-events.ts
git commit -m "refactor: 4 处 baseUrl 读取统一走 getBaseUrl()"
```

---

### Task 4: model-prefs + filter-models

**Files:**
- Create: `agent-mobile-app/src/services/model-prefs.ts`
- Create: `agent-mobile-app/src/services/filter-models.ts`
- Test: `agent-mobile-app/src/services/model-prefs.test.ts`
- Test: `agent-mobile-app/src/services/filter-models.test.ts`

**Interfaces:**
- Produces: `ModelPref = {providerID: string; modelID: string}`;`getModelPref(agent): Promise<ModelPref|null>`;`setModelPref(agent, pref)`;`loadModelPrefs(): Promise<Record<string, ModelPref>>`;`filterModels(list, query): ModelPref[]`

- [x] **Step 1: 写 filter-models 失败测试**

`src/services/filter-models.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { filterModels } from "./filter-models";

const list = [
  { providerID: "deepseek", modelID: "deepseek-v4-pro" },
  { providerID: "deepseek", modelID: "deepseek-v4-flash" },
  { providerID: "deepseek", modelID: "deepseek-v4-lite" },
];

describe("filterModels", () => {
  it("空 query 返回全量", () => {
    expect(filterModels(list, "")).toHaveLength(3);
  });

  it("子串过滤不分大小写", () => {
    const r = filterModels(list, "PRO");
    expect(r).toEqual([{ providerID: "deepseek", modelID: "deepseek-v4-pro" }]);
  });

  it("按 modelID 或 provider 匹配", () => {
    expect(filterModels(list, "flash")).toHaveLength(1);
    expect(filterModels(list, "deepseek")).toHaveLength(3);
  });

  it("无匹配返回空", () => {
    expect(filterModels(list, "xyz")).toHaveLength(0);
  });
});
```

- [x] **Step 2: 确认失败**

Run: `cd agent-mobile-app && pnpm test -- src/services/filter-models.test.ts`
Expected: FAIL `Cannot find module './filter-models'`

- [x] **Step 3: 实现 filter-models**

`src/services/filter-models.ts`:
```ts
export interface ModelPref {
  providerID: string;
  modelID: string;
}

/** 按 modelID/providerID 子串不分大小写过滤,空 query 返回全量 */
export function filterModels(list: ModelPref[], query: string): ModelPref[] {
  const q = query.trim().toLowerCase();
  if (!q) return list;
  return list.filter(
    (m) =>
      m.modelID.toLowerCase().includes(q) ||
      m.providerID.toLowerCase().includes(q),
  );
}
```

- [x] **Step 4: 确认通过**

Run: `cd agent-mobile-app && pnpm test -- src/services/filter-models.test.ts`
Expected: PASS (4 tests)

- [x] **Step 5: 写 model-prefs 失败测试**

`src/services/model-prefs.test.ts`:
```ts
import { vi, describe, it, expect, beforeEach } from "vitest";
const { store } = vi.hoisted(() => ({ store: new Map<string, string>() }));
vi.mock("@react-native-async-storage/async-storage", () => ({
  default: {
    getItem: (k: string) => Promise.resolve(store.get(k) ?? null),
    setItem: (k: string, v: string) => { store.set(k, v); return Promise.resolve(); },
    removeItem: (k: string) => { store.delete(k); return Promise.resolve(); },
  },
}));
beforeEach(() => store.clear());

import { getModelPref, setModelPref, loadModelPrefs } from "./model-prefs";

describe("model-prefs", () => {
  it("set/get 单 agent", async () => {
    await setModelPref("build", { providerID: "deepseek", modelID: "deepseek-v4-pro" });
    expect(await getModelPref("build")).toEqual({ providerID: "deepseek", modelID: "deepseek-v4-pro" });
  });

  it("覆盖单 agent 不影响其他", async () => {
    await setModelPref("build", { providerID: "deepseek", modelID: "a" });
    await setModelPref("plan", { providerID: "deepseek", modelID: "b" });
    expect(await getModelPref("build")).toEqual({ providerID: "deepseek", modelID: "a" });
    expect(await getModelPref("plan")).toEqual({ providerID: "deepseek", modelID: "b" });
  });

  it("loadModelPrefs 返回 map", async () => {
    await setModelPref("build", { providerID: "deepseek", modelID: "a" });
    const all = await loadModelPrefs();
    expect(all.build).toEqual({ providerID: "deepseek", modelID: "a" });
  });

  it("未配返回 null", async () => {
    expect(await getModelPref("design")).toBeNull();
  });
});
```

- [x] **Step 6: 确认失败**

Run: `cd agent-mobile-app && pnpm test -- src/services/model-prefs.test.ts`
Expected: FAIL `Cannot find module './model-prefs'`

- [x] **Step 7: 实现 model-prefs**

`src/services/model-prefs.ts`:
```ts
import AsyncStorage from "@react-native-async-storage/async-storage";
import type { ModelPref } from "./filter-models";

const KEY = (agent: string) => `pulse_model_pref_${agent}`;

export async function getModelPref(agent: string): Promise<ModelPref | null> {
  try {
    const raw = await AsyncStorage.getItem(KEY(agent));
    return raw ? (JSON.parse(raw) as ModelPref) : null;
  } catch { return null; }
}

export async function setModelPref(agent: string, pref: ModelPref): Promise<void> {
  try { await AsyncStorage.setItem(KEY(agent), JSON.stringify(pref)); } catch {}
}

export async function loadModelPrefs(): Promise<Record<string, ModelPref>> {
  try {
    const keys = await AsyncStorage.getAllKeys();
    const out: Record<string, ModelPref> = {};
    for (const k of keys) {
      if (!k.startsWith("pulse_model_pref_")) continue;
      const raw = await AsyncStorage.getItem(k);
      if (raw) {
        const agent = k.slice("pulse_model_pref_".length);
        out[agent] = JSON.parse(raw) as ModelPref;
      }
    }
    return out;
  } catch { return {}; }
}
```

> 注:测试 mock 需加 `getAllKeys`。更新 mock 模板(本文件 + 后续涉及 AsyncStorage 的)在 `default` 加:
> `getAllKeys: () => Promise.resolve([...store.keys()]),`

- [x] **Step 8: 更新本文件 mock + 确认通过**

`src/services/model-prefs.test.ts` 的 mock `default` 加:
```ts
getAllKeys: () => Promise.resolve([...store.keys()]),
```
Run: `cd agent-mobile-app && pnpm test -- src/services/model-prefs.test.ts`
Expected: PASS (4 tests)

- [x] **Step 9: tsc + 提交**

Run: `cd agent-mobile-app && pnpm exec tsc --noEmit`
```bash
cd agent-mobile-app
git add src/services/model-prefs.ts src/services/model-prefs.test.ts src/services/filter-models.ts src/services/filter-models.test.ts
git commit -m "feat: model-prefs 服务 + filter-models 纯函数"
```

---

### Task 5: bff-health 探测

**Files:**
- Create: `agent-mobile-app/src/services/bff-health.ts`
- Test: `agent-mobile-app/src/services/bff-health.test.ts`

**Interfaces:**
- Produces: `probeBffHealth(url: string, timeoutMs?: number): Promise<boolean>`

- [x] **Step 1: 写失败测试**

`src/services/bff-health.test.ts`:
```ts
import { vi, describe, it, expect, afterEach } from "vitest";
import { probeBffHealth } from "./bff-health";

const ok = vi.fn();
afterEach(() => ok.mockReset());

describe("probeBffHealth", () => {
  it("2xx 返回 true", async () => {
    ok.mockResolvedValue({ ok: true, status: 200 });
    expect(await probeBffHealth("http://x", 1000, ok)).toBe(true);
  });

  it("非 2xx 返回 false", async () => {
    ok.mockResolvedValue({ ok: false, status: 500 });
    expect(await probeBffHealth("http://x", 1000, ok)).toBe(false);
  });

  it("抛错返回 false", async () => {
    ok.mockRejectedValue(new Error("net"));
    expect(await probeBffHealth("http://x", 1000, ok)).toBe(false);
  });
});
```

- [x] **Step 2: 确认失败**

Run: `cd agent-mobile-app && pnpm test -- src/services/bff-health.test.ts`
Expected: FAIL `Cannot find module './bff-health'`

- [x] **Step 3: 实现**

`src/services/bff-health.ts`:
```ts
// 第三个参数 fetchImpl 仅供测试注入;生产用全局 fetch
export async function probeBffHealth(
  url: string,
  timeoutMs = 3000,
  fetchImpl: typeof fetch = fetch,
): Promise<boolean> {
  try {
    const ctrl = new AbortController();
    const timer = setTimeout(() => ctrl.abort(), timeoutMs);
    const res = await fetchImpl(`${url.replace(/\/$/, "")}/api/auth/login`, {
      method: "HEAD",
      signal: ctrl.signal,
    });
    clearTimeout(timer);
    return res.ok || (res.status >= 200 && res.status < 500);
  } catch {
    return false;
  }
}
```

> 注:用 `HEAD /api/auth/login`(已知存在端点);任何 2xx/3xx/4xx 视为在线(BFF 活着),仅 5xx/网络失败视为离线。

- [x] **Step 4: 确认通过**

Run: `cd agent-mobile-app && pnpm test -- src/services/bff-health.test.ts`
Expected: PASS (3 tests)

- [x] **Step 5: tsc + 提交**

Run: `cd agent-mobile-app && pnpm exec tsc --noEmit`
```bash
cd agent-mobile-app
git add src/services/bff-health.ts src/services/bff-health.test.ts
git commit -m "feat: bff-health 探测服务"
```

---

### Task 6: 启动读 runtimeBaseUrl(index.tsx)

**Files:**
- Modify: `agent-mobile-app/src/app/(tabs)/index.tsx:68-77`

**Interfaces:**
- Consumes: `getRuntimeBaseUrl` from Task 1
- 无新测试(tsc + 手动验证)

- [x] **Step 1: 改 loadToken useEffect**

import 加(顶部 import 区,若 opencodeConfig 已 import 则补):
```ts
import { getRuntimeBaseUrl } from "@/services/bff-config";
import { opencodeConfig } from "@/config/opencode";
```
改 useEffect(68-77)为先读覆盖值再 loadToken:
```ts
useEffect(() => {
  getRuntimeBaseUrl().then((override) => {
    if (override) opencodeConfig.runtimeBaseUrl = override;
    loadToken().then((tok) => {
      setNeedLogin(!tok);
      if (tok) refresh();
    });
  });
  return onUnauthorized(() => setNeedLogin(true));
}, [refresh]);
```

- [x] **Step 2: tsc + 已有测试**

Run: `cd agent-mobile-app && pnpm exec tsc --noEmit && pnpm test`
Expected: 0 errors;无回归

- [x] **Step 3: 提交**

```bash
cd agent-mobile-app
git add src/app/\(tabs\)/index.tsx
git commit -m "feat: 启动读 runtimeBaseUrl 覆盖 env(方案C重启生效)"
```

---

### Task 7: ChatPanel 集成 model-prefs

**Files:**
- Modify: `agent-mobile-app/src/components/chat/ChatPanel.tsx:395-414`

**Interfaces:**
- Consumes: `loadModelPrefs` from Task 4
- 优先级:Me 偏好 > server `agent.model` > FALLBACK_AGENTS

- [x] **Step 1: 改 listAgents useEffect(395-414)**

import 加:
```ts
import { loadModelPrefs } from "@/services/model-prefs";
```
改 useEffect——拉 listAgents 后再 loadModelPrefs 覆盖:
```ts
useEffect(() => {
  let cancelled = false;
  opencodeClient
    .listAgents()
    .then(async (list) => {
      if (cancelled) return;
      const primary = list.filter((a) => a.mode === "primary" && a.model);
      const prefs = await loadModelPrefs();
      setAgents(
        primary
          .filter((a) => FALLBACK_AGENTS.some((f) => f.id === a.name))
          .map((a) => {
            const p = prefs[a.name];
            return {
              id: a.name,
              model: p
                ? { providerID: p.providerID, modelID: p.modelID }
                : { providerID: a.model!.providerID, modelID: a.model!.modelID },
            };
          }),
      );
    })
    .catch(() => {});
  return () => { cancelled = true; };
}, []);
```

- [x] **Step 2: 改初始 model pill(417-444 的 session adopt useEffect 后加偏好优先)**

在 session adopt useEffect 内,adopt session model 前先查偏好:
```ts
// 初始 model pill:Me 偏好优先,无则 adopt session model(若匹配 primary)
opencodeClient
  .getSession(sessionID)
  .then(async (s) => {
    if (cancelled) return;
    if (s.agent) {
      const idx = agents.findIndex((a) => a.id === s.agent);
      if (idx !== -1) setAgentIdx(idx);
    }
    const prefs = await loadModelPrefs();
    const curAgent = s.agent ?? agents[agentIdx]?.id;
    const pref = curAgent ? prefs[curAgent] : null;
    if (pref) {
      setModel({ providerID: pref.providerID, modelID: pref.modelID });
    } else if (
      s.model?.providerID && s.model?.id &&
      agents.some((a) => a.model.providerID === s.model!.providerID && a.model.modelID === s.model!.id)
    ) {
      setModel({ providerID: s.model.providerID, modelID: s.model.id });
    }
  })
  .catch(() => {});
```

- [x] **Step 3: tsc + 已有测试**

Run: `cd agent-mobile-app && pnpm exec tsc --noEmit && pnpm test`
Expected: 0 errors;无回归

- [x] **Step 4: 提交**

```bash
cd agent-mobile-app
git add src/components/chat/ChatPanel.tsx
git commit -m "feat: ChatPanel 读 model 偏好设默认 pill(优先 server > fallback)"
```

---

### Task 8: Me 页 UI

**Files:**
- Rewrite: `agent-mobile-app/src/app/(tabs)/me.tsx`
- 无单测(UI);验证 tsc + expo export + 手动

**Interfaces:**
- Consumes: `getBaseUrl`/`opencodeConfig`(Task1);`getUsername`/`logout`/`clearUsername`(Task2);`loadModelPrefs`/`setModelPref`/`ModelPref`/`filterModels`(Task4);`probeBffHealth`(Task5);`setRuntimeBaseUrl`/`clearRuntimeBaseUrl`/`getRuntimeBaseUrl`(Task1);`opencodeClient.listAgents/listProviders`

- [x] **Step 1: 实现 me.tsx**

`src/app/(tabs)/me.tsx`(覆盖全文):
```tsx
import React, { useEffect, useState, useCallback } from "react";
import { View, ScrollView, Pressable, StyleSheet } from "react-native";
import { ChevronRight } from "lucide-react-native";
import { colors, spacing, radius } from "@/theme";
import { Card, Text, Button, Input, SearchInput, BottomSheet, StatusDot, Icon } from "@/components";
import { opencodeConfig, getBaseUrl } from "@/config/opencode";
import { getUsername, logout } from "@/services/auth";
import { getRuntimeBaseUrl, setRuntimeBaseUrl, clearRuntimeBaseUrl } from "@/services/bff-config";
import { probeBffHealth } from "@/services/bff-health";
import { loadModelPrefs, setModelPref } from "@/services/model-prefs";
import { filterModels, type ModelPref } from "@/services/filter-models";
import { opencodeClient } from "@/services/opencode-client";

interface AgentRow { id: string; model: ModelPref }

export default function MeScreen() {
  const [username, setUsername] = useState<string | null>(null);
  const [online, setOnline] = useState(false);
  const [addr, setAddr] = useState("");
  const [saved, setSaved] = useState(false);
  const [agents, setAgents] = useState<AgentRow[]>([]);
  const [modelList, setModelList] = useState<ModelPref[]>([]);
  const [pickAgent, setPickAgent] = useState<string | null>(null);
  const [query, setQuery] = useState("");

  const reload = useCallback(async () => {
    setUsername(await getUsername());
    const url = getBaseUrl();
    setOnline(await probeBffHealth(url));
    setAddr(getRuntimeBaseUrl() ? (await getRuntimeBaseUrl())! : opencodeConfig.baseUrl);
    const prefs = await loadModelPrefs();
    const list = await opencodeClient.listAgents().catch(() => []);
    const primary = list.filter((a) => a.mode === "primary");
    setAgents(primary.map((a) => ({
      id: a.name,
      model: prefs[a.name] ?? { providerID: a.model?.providerID ?? "deepseek", modelID: a.model?.modelID ?? "" },
    })));
    const prov = await opencodeClient.listProviders().catch(() => ({ providers: [], default: {} }));
    const flat: ModelPref[] = [];
    for (const p of prov.providers) {
      if (p.id === "openrouter" || p.id === "siliconflow-cn") continue;
      for (const mid of Object.keys(p.models ?? {})) {
        if (!mid.toLowerCase().includes("deepseek")) continue;
        flat.push({ providerID: p.id, modelID: mid });
      }
    }
    if (flat.length > 0) setModelList(flat);
  }, []);

  useEffect(() => { reload(); }, [reload]);

  const doLogout = async () => {
    await logout();
    setUsername(null);
  };

  const saveAddr = async () => {
    await setRuntimeBaseUrl(addr);
    setSaved(true);
  };

  const resetAddr = async () => {
    await clearRuntimeBaseUrl();
    setAddr(opencodeConfig.baseUrl);
    setSaved(false);
  };

  const confirmModel = async (pref: ModelPref) => {
    if (!pickAgent) return;
    await setModelPref(pickAgent, pref);
    setAgents((prev) => prev.map((a) => a.id === pickAgent ? { ...a, model: pref } : a));
    setPickAgent(null);
    setQuery("");
  };

  const filtered = filterModels(modelList, query);
  const current = agents.find((a) => a.id === pickAgent)?.model;

  return (
    <ScrollView style={s.screen} contentContainerStyle={s.content}>
      {/* Card 1 连接与账号 */}
      <Card testID="me-card-account" style={s.card}>
        <View style={s.row}>
          <StatusDot status={online ? "success" : "idle"} size={10} accessibilityLabel={online ? "在线" : "离线"} />
          <Text variant="bodyStrong">{online ? "在线" : "离线"}</Text>
        </View>
        <Text variant="caption" color="muted" style={s.mt}>账号</Text>
        <Text variant="body">{username ?? "未登录"}</Text>
        <Text variant="caption" color="muted" style={s.mt}>BFF</Text>
        <Text variant="body" numberOfLines={1}>{getBaseUrl()}</Text>
        {username ? (
          <View style={s.btnRow}><Button variant="secondary" label="登出" onPress={doLogout} testID="me-logout" /></View>
        ) : (
          <View style={s.btnRow}><Button variant="secondary" label="去 Pulse 登录" onPress={() => {}} testID="me-goto-login" /></View>
        )}
      </Card>

      {/* Card 2 BFF 地址 */}
      <Card testID="me-card-bff" style={s.card}>
        <Text variant="title">BFF 地址</Text>
        <Text variant="caption" color="muted" style={s.mt}>当前生效</Text>
        <Text variant="body">{addr}{getRuntimeBaseUrl() ? "" : " (默认)"}</Text>
        <Text variant="caption" color="muted" style={s.mt}>新地址</Text>
        <Input value={addr} onChangeText={setAddr} placeholder="http://IP:port" testID="me-bff-input" />
        <View style={s.btnRow}>
          <Button label="保存" onPress={saveAddr} testID="me-bff-save" />
          <Button variant="ghost" label="恢复默认" onPress={resetAddr} testID="me-bff-reset" />
        </View>
        {saved && <Text variant="caption" color="accent">ⓘ 保存后重启生效(web 刷新即可)</Text>}
      </Card>

      {/* Card 3 model 偏好 */}
      <Card testID="me-card-model" style={s.card}>
        <Text variant="title">model 偏好</Text>
        {agents.map((a) => (
          <Pressable key={a.id} testID={`me-agent-${a.id}`} onPress={() => { setPickAgent(a.id); setQuery(""); }} style={s.agentRow}>
            <Text variant="body" style={s.agentId}>{a.id}</Text>
            <Text variant="caption" color="muted" numberOfLines={1} style={s.flex}>{a.model.providerID}:{a.model.modelID}</Text>
            <Icon icon={ChevronRight} size="sm" color="muted" />
          </Pressable>
        ))}
      </Card>

      {/* model 选择 BottomSheet */}
      <BottomSheet visible={pickAgent !== null} onClose={() => setPickAgent(null)} testID="me-model-sheet">
        <View style={s.sheetHeader}>
          <Text variant="title">为 {pickAgent} 选择 model</Text>
        </View>
        <SearchInput value={query} onChangeText={setQuery} onClear={() => setQuery("")} placeholder="搜索 model…" testID="me-model-search" />
        <ScrollView style={s.sheetScroll}>
          {filtered.map((m) => {
            const active = current?.providerID === m.providerID && current?.modelID === m.modelID;
            return (
              <Pressable key={`${m.providerID}:${m.modelID}`} testID={`me-model-${m.modelID}`} onPress={() => confirmModel(m)} style={[s.modelItem, active && s.modelItemActive]}>
                <Text color={active ? "accent" : "ink"}>{m.providerID}: {m.modelID}</Text>
              </Pressable>
            );
          })}
        </ScrollView>
        <Button label="确认" onPress={() => setPickAgent(null)} testID="me-model-confirm" />
      </BottomSheet>
    </ScrollView>
  );
}

const s = StyleSheet.create({
  screen: { flex: 1, backgroundColor: colors.canvas },
  content: { padding: spacing.lg, gap: spacing.lg },
  card: { gap: spacing.xs },
  row: { flexDirection: "row", alignItems: "center", gap: spacing.xs },
  mt: { marginTop: spacing.sm },
  btnRow: { flexDirection: "row", gap: spacing.sm, marginTop: spacing.md },
  agentRow: { flexDirection: "row", alignItems: "center", gap: spacing.sm, paddingVertical: spacing.sm },
  agentId: { width: 72 },
  flex: { flex: 1 },
  sheetHeader: { marginBottom: spacing.md },
  sheetScroll: { maxHeight: 300, marginVertical: spacing.md },
  modelItem: { paddingVertical: spacing.sm, paddingHorizontal: spacing.md, borderRadius: radius.sm },
  modelItemActive: { backgroundColor: colors.accent.subtle },
});
```

> 注:`Icon` 的 `size` token 取值见 `primitives/Icon.tsx`;`Card` 接 `style` prop(已支持,见 Card.tsx)。`去 Pulse 登录` 的 onPress 留空(切 tab 由 expo-router 处理,见 Step 2)。

- [x] **Step 2: 「去 Pulse 登录」切 tab 0**

import 加:
```ts
import { useRouter } from "expo-router";
```
组件内:
```ts
const router = useRouter();
```
`去 Pulse 登录` onPress:
```ts
onPress={() => router.replace("/(tabs)/")}
```

- [x] **Step 3: tsc + lint**

Run: `cd agent-mobile-app && pnpm exec tsc --noEmit && pnpm lint`
Expected: 0 errors

- [x] **Step 4: expo export 验证**

Run:
```bash
cd agent-mobile-app
EXPO_PUBLIC_OPENCODE_URL=http://106.13.181.13:19234 pnpm exec expo export --platform web --clear
```
Expected: 导出成功,无运行时错误

- [x] **Step 5: 提交**

```bash
cd agent-mobile-app
git add src/app/\(tabs\)/me.tsx
git commit -m "feat: Me 页 UI——连接与账号/BFF地址/model偏好(三Card)"
```

---

## 验收清单

- [x] `pnpm exec tsc --noEmit` 0 errors
- [x] `pnpm test` 全过(新增 bff-config/auth/filter-models/model-prefs/bff-health 测试)
- [x] Me 页三 Card 渲染正常(连接与账号/BFF 地址/model 偏好)
- [x] model 选择 BottomSheet:SearchInput 过滤 + ScrollView 滚动
- [x] 改 BFF 地址 → 提示重启生效 → 刷新后 `getBaseUrl()` 用新地址
- [x] ChatPanel 默认 pill 读 Me 偏好(优先 server > fallback)
- [x] 登出:token/username 清,回未登录态

## Self-Review

**Spec coverage**: 三块(连接账号/BFF地址/model偏好)→ Task 8 ✓;getBaseUrl 运行时覆盖 → Task 1+3+6 ✓;username 持久化 → Task 2 ✓;model 偏好存取 → Task 4 ✓;ChatPanel 集成 → Task 7 ✓;bff-health → Task 5 ✓;filterModels → Task 4 ✓。

**Placeholder scan**: 无 TBD/TODO;所有代码块完整。

**Type consistency**: `ModelPref` 在 filter-models 定义,model-prefs 重新导出 `import type`,me.tsx/ChatPanel 引用一致;`getBaseUrl`/`getRuntimeBaseUrl`/`setModelPref`/`loadModelPrefs`/`probeBffHealth` 签名各 Task 一致。
